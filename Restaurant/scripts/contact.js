function validateForm() {
	/*
	Bootstrap no longer supports the method of JavaScript validation outlined
	in the course, so validation is being handled by Bootstrap directly.
	
	Bootstrap 4 handles invalid fields before submission, 
	so the form is valid if this function has been called.
	*/ 
	
	alert("Information is valid.");
	
	return false;
}